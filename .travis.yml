language: java
dist: trusty
sudo: false

jdk:
  - oraclejdk9

before_script:
  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V

- os: linux
  env:
    - TEST="Junit"
  script:
    - mvn test -B
- os: linux
  env:
    - TEST="Spotbugs"
  script:
    - mvn spotbugs:check -B
